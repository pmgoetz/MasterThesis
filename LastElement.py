def get_last_element(outcome):
#     print("Outcome", outcome, type(outcome))
    array_length = len(outcome)
    last_element = outcome[array_length - 1]
#     print("Last Element", last_element, type(last_element))
    return last_element